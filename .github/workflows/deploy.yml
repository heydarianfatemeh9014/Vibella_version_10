name: Deploy Flask App

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'  # ورژنی که پروژه‌ات باهاش سازگاره

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: |
        # اینجا می‌تونی تست‌های پروژه رو بذاری اگه داری
        echo "No tests defined"

    - name: Deploy to server or build
      run: |
        # مثلا اگر داره روی Heroku یا سرویس دیگه دپلوی می‌کنی اینجا دستوراتش رو بذار
        echo "Deploy step placeholder"
